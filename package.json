{
  "name": "dictionary",
  "version": "1.0.0",
  "private": true,
  "description": "A dictionary application with internationalisation support",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cleanup":"rm -f \"./tsconfig.tsbuildinfo\"",
    "build": "tsc --build",
    "start": "npm install",
    "test": "tsc --build"
  },
  "dependencies": {
    "pluralize": "^8.0.0",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/pluralize": "^0.0.33"
  }
}
